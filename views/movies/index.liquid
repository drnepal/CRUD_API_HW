{% layout 'layout.liquid' %}
{% block content %}
    <!-- we need to account for two conditions here -->
    <!-- first, if we have fruits in our array -->
    <!-- second, if there are no fruits in the array -->
    {% if movies.length > 0 %}
    <h2>All movies</h2>
        <ul>
            {% for fruit in fruits %}
                <li>{{ movie.name }}</li>
            {% endfor %}
        </ul>
    {% else %}
        <h2>No movies yet... Releasing Soon!</h2>
    {% endif %}
{% endblock %}